apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-cert
  namespace: argocd
spec:
  secretName: argocd-tls
  issuerRef:
    name: letsencrypt-stg
    kind: ClusterIssuer
  commonName: argocd.rubberduckops.com
  dnsNames:
    - argocd.rubberduckops.com
